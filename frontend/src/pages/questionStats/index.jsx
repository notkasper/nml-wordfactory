import React from 'react';
import PageContainer from '../_shared/PageContainer';

const QuestionStats = (props) => {
  return <PageContainer>Hier komen statistieken van een vraag</PageContainer>;
};

export default QuestionStats;
